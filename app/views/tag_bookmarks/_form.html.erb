<%= form_with(model: tag_bookmark) do |form| %>
  <% if tag_bookmark.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tag_bookmark.errors.count, "error") %> prohibited this tag_bookmark from being saved:</h2>

      <ul>
        <% tag_bookmark.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :tag_id, style: "display: block" %>
    <%= form.text_field :tag_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
